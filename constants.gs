function getConstants() {
  const constants = {
    wallet_address_list: '{{your wallet addresses here (comma separated!)}}',
    zapperfi_api_key: '{{your api key here}}',
    terra_luna_address: '{{any terra luna address here (only deals with 1 for now)}}'
  };
  return constants;
}
